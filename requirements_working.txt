# ================================
# WORKING CONFIGURATION - 2025.07.29
# Successfully tested with RTX 3090, CUDA 12.6
# ================================

# Experiment Tracking & Visualization
wandb==0.21.0  # Weights & Biases for experiment tracking

# Core ML Libraries - UPDATED WORKING VERSIONS
torch==2.7.1  # PyTorch with CUDA 12.6 support
torchvision==0.22.1+cu126  # Compatible with PyTorch 2.7.1
torchaudio==2.5.1  # Audio processing
transformers==4.54.0  # Transformer models
datasets==3.6.0  # HuggingFace datasets (updated)
evaluate==0.4.0
pytorch_lightning==2.5.2
unsloth==2025.7.8  # QLoRA support - WORKING VERSION
unsloth-zoo==2025.7.10  # Unsloth extensions

# Quantization & Optimization - UPDATED
bitsandbytes==0.46.1  # 8-bit optimization (updated from 0.41.3)
accelerate==1.9.0  # For distributed training (updated)
peft==0.16.0  # Parameter-efficient fine-tuning

# Additional ML Libraries
xformers==0.0.31.post1  # Memory-efficient attention
trl==0.19.1  # Transformer reinforcement learning

# Data Processing
pandas==2.3.1  # Data manipulation (updated)
numpy==2.1.2  # Numerical computing (updated from 1.23.5)
scikit-learn==1.3.2

# NLP & Text Processing
rouge==1.0.1
rouge-score==0.1.2
nltk==3.8.1
konlpy==0.6.0  # Korean NLP
sentencepiece==0.2.0  # Tokenization (updated)
tokenizers==0.21.4  # Fast tokenizers

# Experiment Tracking
tensorboard==2.15.1

# Utilities
tqdm==4.67.1  # Progress bars (updated)
PyYAML==6.0.2  # Configuration files (updated)
python-dotenv==1.0.0
jsonlines==4.0.0
protobuf==6.31.1  # Protocol buffers

# API & Web
aiohttp==3.12.14  # For async requests (updated)
requests==2.32.4  # HTTP requests (updated)

# Development Tools
jupyter==1.0.0
jupyterlab==4.0.9
ipywidgets==8.1.1
matplotlib==3.8.2
seaborn==0.13.0
gradio==5.38.2  # UI for model demos (updated)
gradio-client==1.11.0

# Code Quality
black==23.12.1
flake8==7.0.0

# System & Performance
psutil==7.0.0  # System monitoring (updated)
gpustat==1.1.1  # GPU monitoring
py-cpuinfo==9.0.0

# Optional: Backtranslation
googletrans==4.0.0-rc1  # Google Translate
# sentencepiece already listed above

# Optional: Advanced Features
deepspeed==0.12.6  # For optimization

# NVIDIA CUDA Dependencies (automatically installed)
# nvidia-cublas-cu12==12.6.4.1
# nvidia-cuda-cupti-cu12==12.6.80
# nvidia-cuda-nvrtc-cu12==12.6.77
# nvidia-cuda-runtime-cu12==12.6.77
# nvidia-cudnn-cu12==9.5.1.17
# nvidia-cufft-cu12==11.3.0.4
# nvidia-cufile-cu12==1.11.1.6
# nvidia-curand-cu12==10.3.7.77
# nvidia-cusolver-cu12==11.7.1.2
# nvidia-cusparse-cu12==12.5.4.2
# nvidia-cusparselt-cu12==0.6.3
# nvidia-nccl-cu12==2.26.2
# nvidia-nvjitlink-cu12==12.6.85
# nvidia-nvtx-cu12==12.6.77

# Core Python Dependencies
setuptools==70.2.0
wheel==0.45.1
packaging==25.0
typing-extensions==4.14.1

# ================================
# INSTALLATION NOTES:
# 1. Use uv for package management: uv pip install -r requirements.txt
# 2. Ensure CUDA 12.6 is available on the system
# 3. For torchvision, use: uv pip install torchvision==0.22.1+cu126 --index-url https://download.pytorch.org/whl/cu126
# 4. This configuration has been tested and confirmed working with unsloth
# ================================
